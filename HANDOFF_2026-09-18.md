# HANDOFF 2026-09-18 — Submodular Maximization from Predictions（ICLR 2027）

本文件取代 HANDOFF_2026-09-14.md。目的：让一个全新的对话读完后，能 exactly 接上现在的工作：写 theory 部分（第 3–6 节）。
新对话的第一件事是读完本文件（全部，不跳），然后按 §0 的握手协议回复。

---

## §0 新对话的握手协议（必须先做）

读完本文件后，第一条回复只做三件事，不做其他：
1. 用五句话复述当前状态（写作到了哪、理论验证到了哪、下一步是什么）。
2. 回答四个校验问题：
   - Definition 1 的约定（方案二）是什么，它为什么让 hook 图里 B 那类 surrogate 的 η 接近 1？
   - Figure 1 的 toy instance 的六个真值和 A、B 的六个预测值是多少，A、B 各走出什么集合、拿到多少？
   - J6、J7 的验证结果是什么，哪几步仍是 [HAND-PROOF-UNREVIEWED]，谁来读、读哪两个附录？
   - intro 的四段式各段说什么，predictive greedy 这个名字在哪一句第一次出现、怎么说才不越界？
3. 问 Cici："从 §11 的哪一条开始？"

不要在第一条回复里提出新想法、新任务或新写作。

---

## §1 人与沟通方式

- Cici（Zhiyun Jiang），悉尼大学三年级博士生，导师 Albert Zomaya。**这篇论文 Cici 全权拍板。** Tianming Zhao 是作者（idea 是他的），但不参与写作，也不再做人工复核（论文已离他的原稿太远）。Cici 是唯一的人类复核者。
- 中文沟通，术语、定理陈述、论文正文用英文。简洁，少格式，不用 em dash，不用 genuinely/honestly，简体字。
- 她要直接判断：完成就说完成，没完成就说没完成。解释先数字后公式，高中生能懂；图要极简。
- 数学必须渲染：文件优先 HTML（MathJax，cdnjs）；聊天里用 $…$。长内容让她上传文件，不粘贴。给 Claude Code 的文件必须作附件发。
- 她会把 GPT 输出贴来裁定，格式三堆：它对要改 / 它错附证据 / 需要 oracle 裁决。GPT 做独立审计和写作平行草稿，Claude 做执行、验证、入库、红队；审稿者和作者不能是同一个系统。
- 铁律：数学断言的状态由 oracle 决定，不由信心决定。标签：[VERIFIED-SYMBOLIC] [VERIFIED-LP] [VERIFIED-EXHAUSTIVE] [HAND-PROOF-UNREVIEWED] [CONJECTURE] [OPEN] [FAILED]。AI 手证一律 UNREVIEWED，直到自写管线独立验证且一位作者读过（现在这位作者是 Cici）。
- 文献不确定标 [CITATION-NEEDS-VERIFICATION]，绝不编造。
- 写作分工：Cici 写正文散文，逐词定稿；Claude 起草、红队、结构、机械段落。定理陈述逐字从 THEOREM_LEDGER 抄，证明用仓库 appendix_proofs.tex 重排，不重写。
- 效率教训（这次对话反复出现）：一轮一件事；她定了的事直接执行不再给选项；答一句问一句，要长她说"展开"；不在 Model 里替 intro 说话。
- 写作风格：Strunk，每个词都不能删。每段一行源码（Overleaf 里粘贴不乱）。为省空间她有意用 \( \) 行内公式，不改。

---

## §2 目标、截稿、投稿卫生

- ICLR 2027，冲 oral。判断：新颖性够 oral 水平（一句话可复述且出人意料，完整刻画），但要同时做到：J6/J7/J8 过验证、四句脉络第一页立住、ML 关联显性化。做不到就是好 poster。
- 截稿：abstract 9 月 18 日 AOE（她已提交一版占位，**必须确认 OpenReview 上现在是新摘要不是 TPAMI 旧摘要，占位摘要会被移除**；标题与 PDF 必须一致）；全文 9 月 25 日 AOE；摘要截稿后不能加作者，Tianming 需在列表里且有 OpenReview 账号。
- 标题未定：现用 "Submodular Optimization with Predictions"（TPAMI 旧题），Claude 建议改成带结论的题（候选 "The Exact Cost of Prediction Error in Submodular Maximization"）。Model 开头句与题目要一致。
- AI-use statement：如实申报 AI 辅助（起草、某些构造的初稿、符号验证），"under author-specified directions"。这不是 auto research：方向、判断、收敛都是 Cici。
- TPAMI 原稿从未挂 arXiv，双盲下不可引用不可提及；边际增益误差模型作为本文的模型陈述；经典界归 Goundan–Schulz 2007。
- 原稿 PDF（Submodular_ICLR.pdf）和 THEOREM_LEDGER 已上传到 Project files，新对话可直接读（view /mnt/project 或 project_knowledge_search）。

---

## §3 已定的术语与约定

- 对象叫 **surrogate** $\tilde f$；它给出的值叫 predictions；误差叫 prediction error / marginal-gain error；算法叫 **predictive greedy**。"greedy" 单独出现指经典 greedy（查 $f$）。
- 真目标 $f$ 叫 objective；$f(S)$ 叫 objective value；$f(\mathrm{ALG})/\mathrm{OPT}$ 叫 approximation ratio。
- 值空间的误差叫 **function-value error**（$\max_S|\tilde f(S)-f(S)|/f(S)$），增益空间的叫 marginal-gain error。不用裸的 "value"。
- Definition 1（**方案二**，已定）：$\eta_u,\eta_o>0$，$\eta=\eta_u\eta_o\ge1$，对所有 $S$、$e\notin S$：$d_e(S)/\eta_u\le\tilde d_e(S)\le\eta_o d_e(S)$。缩放不变：$\tilde f$ 乘 $c$ 使 $(\eta_u,\eta_o)\to(c\eta_u,\eta_o/c)$，$\eta$ 不变；$\tilde f=cf$ 的 $\eta=1$。两个因子是误差带的两侧，没有结论依赖拆分，全文只用 $\eta$。
- 方案二的连带改动（**要进台账，标 HAND-PROOF-UNREVIEWED，附录证明跟着改**）：
  - Prop 2(ii)：$\tilde f=(1+M)f$ 的全局 $\eta=1$（台账写的是 $\eta^{\mathrm{sel}}=1$）。
  - Prop 2(iii)：台账版带 $\eta_o<2$ 前提、level $\max\{1-1/\eta_u,\eta_o-1\}$；新约定下正确陈述是"存在正缩放使 $\tilde f$ value-accurate at level $(\eta-1)/(\eta+1)$"，前提消失。证明两行：沿链求和得 $f/\eta_u\le\tilde f\le\eta_o f$，取 $c=2\eta_u/(\eta+1)$。
  - Prop 1 里 $f(O^\ast)$ 写成 OPT。
- $K$：Model 写 $1\le K\le n$；需要 $K\ge2$ 的定理各自带前提；Corollary 处加一句 "at $K=1$ every bound equals $1/\eta$"。
- 三把尺子：$\eta^{\mathrm{sel}}\le\eta^{\mathrm{tr}}\le\eta$（证明的，三行）。$\rho_K$ 只对全局 $\eta$ 说；对 $\eta^{\mathrm{sel}}$ 只说 $L_K$。tr = trajectory，sel = selection；旧文件里的 $\eta^{\mathrm{path}}$ 就是 $\eta^{\mathrm{tr}}$。
- $\eta$ 不管翻不翻转，管翻错的代价：翻错时 $d_e\ge d_o/\eta$。"$\eta$ 小 ⟹ 排序对"是假的（1.03/1.04 反例）。

---

## §4 定理与验证状态（以仓库 THEOREM_LEDGER 为准，这里是速查）

正文编号（按四问脉络）：
- Prop 1 (prop:nobound)：无 $\eta$ 上界则任意确定性算法、$n\ge2K$，存在实例使 $f(T)\le\frac{K}{n-K}\mathrm{OPT}$。[HAND-PROOF]
- Prop 2 (prop:valueacc)：value accuracy 既不充分也不必要，(i)(ii)(iii)，(ii)(iii) 按方案二改写（§3）。
- Thm 1 信息价格 (thm:ceiling)：不限查询确定性天花板 $1/\eta$（$n\ge2K$），穷举 $\tilde f$ 达到；$n<2K$ 时 $K/((2K-n)+(n-K)\eta)$，"穷举达到"方向在仓库仍 [CONJECTURE]，J5 两套外部证明待入库转录。
- Prop 3 (prop:guarantee)：$f(T)\ge L_K(\eta^{\mathrm{sel}})\mathrm{OPT}$，$L_K(x)=1-(1-\frac{1}{xK})^K$，essentially due to Goundan–Schulz 2007（Theorem 编号待核原文）。Thm D 紧性（对 $\eta^{\mathrm{sel}}$、$\eta^{\mathrm{tr}}$ 逐 $K$ 紧）降为 remark。
- Lemma (lem:coherence)：$d-g/\eta\ge(1-1/\eta)(g-h)\ge0$，正文唯一新引理。
- Thm 2 精确价格 (thm:exact)：$k_1=(K-1)\eta+1$，$q=(K-1)\eta/k_1$，$V_j=1-q^j(1-\frac{K-j}{K\eta})$，$\rho_K=\min_{0\le j\le K-1}V_j$，分段点整数 $2..K$，$\eta\ge K$ 时 $1/\eta$；[VERIFIED-SYMBOLIC/LP]。$K=2$：$\min\{1/\eta,3/(2(\eta+1))\}$。Cor：$\rho_K\to1-e^{-1/\eta}$，关于 $K$ 非增。
- Thm 3 同预算精确最优 (thm:linear-exact, J6)：$\mathcal A_{\mathrm{lin}}$（$\le nK$ 次、$|S|\le K$）内确定性算法最优最坏比恰为 $\rho_K(\eta)$，$n\ge4K^5$（可紧到约 $K^3(K-1)^2/2+K^2$）；**TASKS10 Q4 已过**：自写管线复跑 13 恒等式 + 159 组电池 + 34 条分支不等式 [VERIFIED-SYMBOLIC/LP]，结论对每个 $n\ge4K^5$ 精确（不需 $n\to\infty$）。装配步骤（count-grid→集合函数、transcript 归纳、随机平均）与两条初等不等式仍 [HAND-PROOF-UNREVIEWED]，**Cici 要读 app:greedybudget**。
- Thm 4 多项式 hardness (thm:hardness)：$n^c$ 次、$|S|\le K$ 的确定性算法 $\le H_{K,\tau}(\eta)=1-(1-\frac{1}{\eta(K-\tau)+1})^K$，$\tau=\lceil c\rceil+1$。
- Prop (J7, thm:linear-anysize)：任意大小查询、$O(nK)$ 次：$\le W_K(\eta)=\rho_K+\frac{(K-j)Q(m-\eta(K-1))}{K\eta[\eta(\nu^m-1)-m]}<\rho_K+\frac{1}{K(e^{K-1}-K-1)}$（$K\ge3$），$\nu=\eta/(\eta-1)$，$m$ 是 $\Psi(t)=(K\eta-t-1)\nu^t-K(\eta-1)$ 的唯一正根上取整，$Q=q^j$；$K=3,\eta=3/2$：$523/928$。**TASKS10 Q4 已过**：148 项 0 FAILED，99 组电池，F3 反例复现，candidate-bound 段已删。装配步骤 [HAND-PROOF-UNREVIEWED]，**Cici 要读 app:hardness-anysize**。新 must-not-claim：$n<K+t^\ast$ 时反向 greedy 能精确找回 $O$，所以 $n\to\infty$ 量词不可去。
- Prop (J8, ProbeLottery)：$K=2,\eta=3/2$，随机算法，$\le9n$ 次、$|S|\le5$，每个实例 $\mathbb E[F]/\mathrm{OPT}\ge3/5+1/400000$；证明 J6 的 $|S|\le K$ 限制必要（对随机、$\frac92nK$ 预算）。Claude 手推通过、GPT 脚本复现、实现在紧实例得 $3/5+1/2048$。状态 [HAND-PROOF-UNREVIEWED]。**Q5 是否已跑未确认**（REPORT 里没看到），落笔前查 results/J8。确定性版本与 $K\ge3$ [OPEN]。
- 附录：Prop G 刚性、$U_K=V_K=1-q^K$、Remark L（submodular 预测器，$\rho^{\mathrm{sub}}_4(3/2)=23/41$）、三把尺子对照、PE₁ 反例、F3 已删。
- 待确认入库：J5 两套（results/J5_hardcore/、results/J5/）是否已跑并 push；J6→results/J6/linear_exact.md，J7→results/J7/linear_anysize.md，J8→results/J8/probe_lottery.md + J8_claude_spotcheck.py。看 git log。

四档梯子（第 5 节的表，$K=3,\eta=3/2$）：不限查询 $1/\eta=0.667$；多项式次 $|S|\le K$：$H\to1-e^{-1/\eta}$（$K\to\infty$ 0.487）；$O(nK)$ 任意大小：$\le523/928=0.5636$（J7），$K=2$ 时严格 $>\rho_2$（J8）；greedy 自己的预算：恰 $9/16=0.5625$（J6）。第二、三行不是包含关系。

---

## §5 叙事（已定）

核心问题：真目标不可查询，只能查有误差的 surrogate；给定预测质量 $\eta$ 和查询预算，最多能保证多少真实效用，predictive greedy 是否达到。

四问链（第 3–6 节的骨架）：问 0 为什么是 $\eta$（Prop 1、2，在 Model）；问 1 信息价格 $1/\eta$（Thm 1）；问 2 greedy 的精确价格 $\rho_K$（Prop 3、Lemma、Thm 2、Cor）；问 3 差距归谁（夹逼式 $\rho_K\le\alpha^\ast\le1/\eta$，Thm 3、Thm 4、Prop J7/J8，四行表）；问 4 实践用哪个 $\eta$（三把尺子，接实验）。节标题用问题。实验节按同样四问分小节。

带走的故事：Prediction error limits what any algorithm can recover. We determine that limit exactly, determine predictive greedy's worst case exactly, and then ask whether the gap between the two is the algorithm's fault or the budget's. The answer separates the losses that only better predictions can fix from the losses that better optimization could fix.

与前人的关系（核过原文，见 §8）：BRS、Horel–Singer、Hassidim–Singer 三条线的假设全部落在集合的值上，没有一条约束边际增益；他们的否定构造正是利用这个自由度。我们不反驳 BRS，BRS 是价格曲线 $\eta\to\infty$ 的端点。不能写"他们量错了"，写"他们的假设不约束 greedy 使用的量"。

定位：LAA 框架的 prediction-only 档（真目标永不可见，和 scheduling 的差别是算法收不到反馈），consistency 与 smoothness 成立，robustness 不存在且是定理（Prop 1）。不是 learning theory 工作（我们不证可学性）。ML 关联：现代 ML 优化学出来的代理（reward model、LLM-as-judge、数据选择打分）；ICLR 自己的 LLM 数据选择线（SPICE ICLR 2026、Fan et al. ICLR 2025、MIG）优化的就是 submodular 代理。软肋要写进 limitation：我们没证学出来的 surrogate 有小 $\eta$；实验里 raw 路径误差被量化噪声撑大，需加性修正。

页数：intro 2 页（四段式含 related work）、Model 1 页、theory 3.5 页、实验 2 页、结论 0.5 页。

---

## §6 intro 四段式，逐句内容（已定，措辞 Cici 写）

**段一：背景、领域、问题、已有答案、转折**
1. 现代 ML 越来越多的优化对象是学出来的代理，不是无法评估的真目标 [reward model、LLM-as-judge、数据选择打分]。
2. 代理质量直接决定优化质量，"用代理优化"值得当问题研究。
3. 我们研究最基本的一类：子集选择，目标常有 submodular 结构 [krause2014submodular；wei2015；mirzasoleiman2020；killamsetty2021]（不夸大："often submodular, or optimized through a submodular proxy"）。
4. 问题：真目标只能通过代理访问时，还能优化吗，能到什么程度？
5. 已有回答偏否定：BRS 证明从学 $f$ 的样本一般无法优化 $f$，即使可学 [balkanski2017]。
6. 因为可学性保证是分布性的（PMAC 的"大多数集合"），最坏集合不受控；在所有集合上控制函数值也不够 [horel2016]。
7. 噪声模型能拿回 $1-1/e$，但只因随机误差能平均掉；学出来的代理错得是系统性的 [hassidim2017；bhawalkar2026unified]。
8. 转折（However）：这个否定取决于误差量在哪里；量在 greedy 真正使用的量上，答案就变了。下一段用例子说明。

**段二：toy example，落到 research question**
9. 引入图 1：三个元素，$K=2$，一个真目标，两个代理，只能查代理。跑的是经典 greedy 换成代理，**为行文方便称 predictive greedy，不是新算法**；用它是因为 $f$ 可查时它最优，任何正面答案必须先覆盖它 [NWF；NW78]。
10. 惊讶句：function-value error 30% 的 B 拿满 OPT，10% 的 A 只拿 0.74。
11. 说明：function-value error 不决定成败；前人悲观结论量的正是它，是 greedy 不使用的量。
12. 原因：greedy 每步只比较边际增益取最大；A 的小误差恰好翻转了第一次比较（$c$ 被抬过 $a$），B 的大误差没翻转任何一次。
13. 结论句：函数值准既不充分也不必要，greedy 需要的是边际增益的比较正确。
14. 半句桥：边际增益是 submodular 问题的坐标（递减收益本身是关于它的），不是 greedy 的量；光要求"比较正确"给不出保证，因为翻错代价取决于两个增益差多少；限制每个增益的乘性扭曲，翻错的代价就有上界。（不出现 $\eta$ 符号和不等式。）
15. research question（斜体）：给定边际增益误差和查询预算，任何算法最多能保证多少 OPT，predictive greedy 是否达到。

**段三：Related work（1.1）**
16. 四组各一句"误差是什么、答案是多少、差别"：近似 submodular / approximate oracle（Horel–Singer；Das–Kempe；Goundan–Schulz）；噪声（Hassidim–Singer；Bhawalkar）；从样本优化（BRS）；learning-augmented（Purohit；Lykouris；预测动态 Agarwal–Balkanski；预测解 Antoniadis et al. ICLR 2025）。
17. gap：量在边际增益上，GS 的经典分析立刻给一个界，但紧不紧、别的算法能否更好，都不知道。
18. 对照 $\eta=1$：经典理论对三问有精确答案（NWF；NW78 每个预算的最好保证）；$\eta>1$ 三问都没答案，还多一问（哪个误差能测、能作证书）。

**段四：Our contribution（1.2）**
19. 框架半句加指回："We answer it in the prediction-only regime of the learning-augmented framework (Section 2)"。
20. 四条贡献各一句加定理名；明说本文不提出新算法，研究标准算法在代理下的精确表现。
21. "first" 只用在"第一个在边际增益误差模型下给出精确刻画"。
22. LAA 三件套：consistency 在 $\eta=1$ 回到 NWF，smoothness 是 $\rho_K(\eta)$，robustness 不存在且是定理。
23. 收：BRS 的不可能是价格曲线 $\eta\to\infty$ 的端点，不是被反驳，是被解释。

边界：段一到段五的内容 Model 不再说；$\eta$ 的符号只在第 14、15、20 句附近以文字出现。"surprisingly"只说"误差大的赢了"这一事实，不滑成"误差大反而好"。

---

## §7 Figure 1（hook）——已定，数据已验证

$N=\{a,b,c\}$，$K=2$，$f$ 单调 submodular（验证过），OPT $=\{a,b\}=9.5$，真值 greedy 唯一走 $a\to b$（singleton 5.0 > 4.8 > 4.5，无平局，堵住 tie 漏洞）。

| | $\{a\}$ | $\{b\}$ | $\{c\}$ | $\{a,b\}$ | $\{a,c\}$ | $\{b,c\}$ | $\{a,b,c\}$ | max function-value error |
|---|---|---|---|---|---|---|---|---|
| true $f$ | 5.0 | 4.5 | 4.8 | 9.5 | 7.5 | 7.0 | 9.7 | — |
| surrogate $A$ | 4.5 | 4.4 | 5.28 | 9.0 | 6.9 | 7.6 | 9.2 | 10% |
| surrogate $B$ | 6.5 | 3.2 | 3.9 | 12.35 | 8.3 | 5.2 | 12.6 | 30% |

- A：第一步预测 4.5, 4.4, 5.28 → 选 $c$；第二步 $a$: 6.9−5.28=1.62，$b$: 7.6−5.28=2.32 → 选 $b$；$\{b,c\}=7.0$，0.74 OPT。两步都选错。
- B：第一步 6.5, 3.2, 3.9 → 选 $a$；第二步 $b$: 12.35−6.5=5.85，$c$: 8.3−6.5=1.8 → 选 $b$；$\{a,b\}=9.5$ = OPT。B 的误差有正有负（$a$ +30%，$b$ −29%，$c$ −19%）。
- 全局 $\eta$：A ≈ 2.16，B ≈ 4.6（**B 更大**）；$\eta^{\mathrm{sel}}$：A 1.23，B 1.00。**所以图只能说 function-value error 不是决定量、比较才是；不能说"边际误差小的赢"，不能在图里标 $\eta$。** 预测值可以大于 OPT（12.35 > 9.5），因为它不是 $f$ 的值。
- 图的分工：Settings 给真值（读者的上帝视角，greedy 看不到）；Greedy selection 全是预测（greedy 看到的），两步各重算增益、候选递减；Results 用真值结算。
- 文件（outputs）：panel1_setting、panel2a_selection_A、panel2b_selection_B、panel3_result、legend、algo_box（svg/png/pdf），高度统一 2.3–2.45in，配色薄荷 #9FD8CB（true）、奶油黄 #FBE3B9（A 行 / greedy pick 高亮，注意同色两义，Cici 决定保留）、珊瑚粉 #F2A29B（B / 预测柱描边）、覆盆子 #C96B75（名次）。Cici 在 draw.io 里拼三栏（1. Settings / 2. Greedy selection / 3. Results），加 Predictive greedy 伪码框和竖排图例。她拼的某一版 A/B 标签对调过，核对：A 终点 $\{b,c\}$，B 终点 $\{a,b\}$。
- 图注草稿在对话里（"Greedy compares marginal gains, not values. …"），结论句放正文段二，图注可中性。
- 面板标题只写 "surrogate A (max function-value error 10%)"，不写 ordering broken/preserved、不写 even/uneven、不写 ×倍数。
- LaTeX：\includegraphics[width=\textwidth]{figures/hook.pdf}，draw.io 导 PDF 勾 Crop；trim 语法 trim={l b r t}, clip。

---

## §8 Model 节（已定稿的 LaTeX，除开头句）

顺序：opening → Problem definition → Surrogate interface → Algorithm and query model → Error model（含 Prop 1、2，已合并）。

**Opening（未定，Cici 对所有版本都不信服，暂用占位）**：候选 "Greedy never reads a set value; it only compares marginal gains, so that is where we measure a surrogate's error. We model the problem through three objects: the cardinality-constrained submodular maximization problem, a surrogate $\tilde f$ of the objective $f$, and a multiplicative error $\eta$ between their marginal gains." 另一候选把理由落在问题坐标上："The structure of a submodular objective lies in its marginal gains: diminishing returns is a statement about them, and greedy, the canonical algorithm, uses nothing else." 等 intro 写出来再定。

**Problem definition**（定稿；经典参照点那句是否保留由 Cici 定）：
We study monotone submodular maximization when the objective cannot be evaluated. Let $N$ be a ground set of $n$ elements and $f:2^N\to\mathbb{R}$ the objective. For $S\subseteq N$ and $e\in N\setminus S$, define the marginal gain $d_e(S)=f(S\cup\{e\})-f(S)$. We assume that $f$ is normalized, $f(\emptyset)=0$; monotone, $d_e(S)\ge0$ for all $S$ and $e\notin S$; and submodular, $d_e(S)\ge d_e(T)$ whenever $S\subseteq T\subseteq N$ and $e\notin T$. Given an integer budget $1\le K\le n$, the problem is $\max_{S\subseteq N}\{f(S):|S|\le K\}$, $\mathrm{OPT}:=\max_{|S|\le K}f(S)$. [可选：When $f$ can be evaluated, the answer is classical and exact: greedy attains $1-(1-1/K)^K>1-e^{-1}$ of OPT \citep{greedySubmodularFunctions}; for fixed $K$, no algorithm with $O(n)$ computations guarantees more, and every polynomial budget has its own exact best guarantee \citep{nemhauser1978best}.] We study it in the learning-augmented framework \citep{purohit2018improving, lykouris2021competitive}, in its prediction-only regime: the algorithm receives no evaluation of $f$, during or after selection, and has access only to a surrogate $\tilde f$ that returns $\tilde f(S)$ for any set $S$. It selects a feasible set $S$ from these predictions. Its quality is nevertheless the objective value $f(S)$: guarantees are stated over all $(f,\tilde f)$ satisfying the error condition below, and experiments evaluate $f$ offline with held-out ground truth.

**Surrogate interface**（定稿）：
The surrogate is a set function $\tilde f:2^N\to\mathbb{R}$ with $\tilde f(\emptyset)=0$, evaluable on any set, with marginal gains $\tilde d_e(S)=\tilde f(S\cup\{e\})-\tilde f(S)$. We place no structural assumption on $\tilde f$ beyond the error condition below; in particular, $\tilde f$ need not be submodular, since a trained model or a cross-validation estimate is not guaranteed to be. Its monotonicity follows from Definition~\ref{def:eta}. Every assumption is placed on the distance between $\tilde f$ and $f$, defined in Definition~\ref{def:eta} below.

**Algorithm and query model**（定稿）：
In our setting, predictive greedy starts from $S^0=\emptyset$ and, for $t=0,\dots,K-1$, adds $e_t\in\arg\max_{e\notin S^t}\tilde d_e(S^t)$, giving $S^{t+1}=S^t\cup\{e_t\}$. Predictive greedy is the greedy algorithm of \citet{greedySubmodularFunctions} run on the surrogate; greedy with approximate steps was analyzed by \citet{goundan2007revisiting}. The name marks only that the algorithm queries $\tilde f$. Predictive greedy always runs $K$ steps and breaks ties adversarially. An algorithm is an $\alpha$-approximation, $\alpha\in(0,1]$, if the output $S$ satisfies $f(S)\ge\alpha\cdot\mathrm{OPT}$ on every instance of the model (Definition~\ref{def:eta}). Algorithms evaluate only $\tilde f$; each evaluation is one query. A class of algorithms is described by two numbers: the number of queries as a function of $n$ and $K$, and the largest set queried. Predictive greedy makes $nK-K(K-1)/2$ queries, each on a set of size at most $K$.
（不写伪码；不写 "we propose"；不预告第 5 节；"每步是 GS 意义下的 $1/\eta$-approximate step" 放第 4 节 Prop 3 处。）

**Error model**（定稿，含 Prop 1、2）：
The surrogate's error is measured on marginal gains, multiplicatively and in both directions. [Definition 1 如 §3。] The definition forces $\tilde d_e(S)=0$ exactly when $d_e(S)=0$. The two factors describe the two sides of the band, underestimation and overestimation. Only their product matters: rescaling $\tilde f$ by a positive constant changes no greedy decision and moves $\eta_u,\eta_o$ by reciprocal factors, so no result depends on the split, and a comparison between two elements is distorted by at most $\eta_u\eta_o$. Thus $\eta=1$ means every marginal gain is correct up to scale, and every statement below uses $\eta$. Since $\eta$ is a property of the surrogate alone, guarantees in $\eta$ apply to every algorithm, and predictive greedy need not know $\eta$. The multiplicative form follows learning-augmented scheduling \citep{SchedulingViaWeights, FlowTimeScheduling, RealTimeSchedulingwithPredictions}. Two facts place the assumption on marginal gains rather than on values. [Prop 1 陈述；Prop 2 陈述 (i)(ii)(iii) 按 §3。] Proposition~\ref{prop:nobound} says no guarantee independent of the error exists. Proposition~\ref{prop:valueacc} says value accuracy is neither sufficient (i) nor necessary (ii), while a bounded marginal-gain error implies value accuracy up to scale (iii) and not conversely; the marginal-gain condition is the stronger one, and the one the problem responds to. Definition~\ref{def:eta} is the only assumption on the surrogate in the rest of the paper.

Notation table：附录 app:notation，表格已给（Problem / Surrogate and error / Algorithm and queries / Guarantees 四组）。

---

## §9 引用（已核）与 .bib 键

原稿 .bib 已有的键：greedySubmodularFunctions（NWF 1978）、setCoverisHard（Feige 1998）、submodularOptNoise（Hassidim–Singer 2017）、horel2016maximization、approxSubmodularFunctions（Goemans 2009）、learnValueFunctions（Balcan 2012）、representationApproxLearnSubmodularFunctions（Feldman 2013）、submodularFunctionsLearn（Balcan–Harvey 2018）、ImproveOnlineAlgorithmsViaML（Purohit 2018）、cachingWithPredictions（Lykouris JACM）、learningAugmentedSubmodularUpdates（Agarwal–Balkanski 2024）、SchedulingViaWeights、FlowTimeScheduling、RealTimeSchedulingwithPredictions、Krause_Golovin_2014。正文草稿里用了新键（purohit2018improving、lykouris2021competitive、horel2016、hassidim2017、balkanski2017、goemans2009、feldman2013、balcan2018、krause2014submodular），二者取一统一，否则出问号。

新加并核过的条目：nemhauser1978best（MOR 3(3):177–188，DOI 10.1145/moor.3.3.177，只核了摘要）；goundan2007revisiting（Optimization Online 2007/08/1740，working paper，Cici 的条目）；wei2015（ICML 2015 pp. 1954–1963，页码待核）；mirzasoleiman2020（ICML 2020 PMLR 119:6950–6960）；killamsetty2021glister（AAAI 2021 35(9):8110–8118）；bhawalkar2026unified（Bhawalkar, Cai, Feng, Liaw, Lin，NeurIPS vol 38 pp. 166355–166383，Cici 核过）；antoniadis2025approximation（Antoniadis, Eliás, Polak, Venzin，ICLR 2025，预测解，Cici 核过；handoff 旧记的 Cohen-Addad 归属错误，作废）；balkanski2017（STOC 2017 pp. 1016–1027；JACM 69(3) 2022）。

核过的原文事实（可直接用）：
- NW78：固定 $K$，$O(n^{q+1})$ 计算量内最好保证 $1-\frac{K-q}{K}(\frac{K-q-1}{K-q})^{K-q}$，穷举 $q$-集再 greedy 达到；"多项式查询超不过 $1-(1-1/K)^K$"对固定 $K$ 是假的，只对 $O(n)$ 预算（$q=0$）成立。
- Horel–Singer：$(1-\varepsilon)f(S)\le F(S)\le(1+\varepsilon)f(S)$ 对所有 $S$；$\varepsilon>n^{-1/2}$ 任何算法指数下界；greedy 需 $\varepsilon<1/k$，阈值紧。
- Hassidim–Singer：$\tilde f(S)=\xi_S f(S)$，$\xi_S$ i.i.d.，persistent；对抗噪声无非平凡保证。
- BRS：样本 $(S_i,f(S_i))$，PMAC 值空间且分布性（$1-\epsilon$ 大多数集合）；coverage 无常数近似；$n^{-1/4+\epsilon}$（coverage 均匀）、$n^{-1/3+\epsilon}$（一般）。
- PMAC 定义：$\Pr_{\text{samples}}[\Pr_{S\sim\mathcal D}[f(S)\le f^\ast(S)\le\alpha f(S)]\ge1-\epsilon]\ge1-\delta$。
- GS 2007：$f$ 已知，近似在"找最大"一步（计算原因），$\alpha$-approximate incremental oracle；我们是信息原因，Definition 1 蕴含每步是 $1/\eta$-approximate step，故其定理黑箱可用。
- Tianming 原稿：算法自称 NWF 的直接应用（不新），漏引 GS，把分析当新；原稿无任何 any-algorithm 结果，无查询大小限定；$|S|\le K$ 是我们 hardness 构造引入的。
- 近三年顶会 submodular：NeurIPS 2025（Bhawalkar 噪声；Online Two-Stage）、ICML 2025 spotlight（procurement auctions）、ICML 2026（dynamic consistent）、ICLR 2025（k-submodular；Fan et al. 数据选择）、ICLR 2026 SPICE；STOC 2024/25、FOCS 2024（Buchbinder–Feldman）、STOC 2025 Dütting 等、STOC 2026 Poisson process。没人做"目标由带对抗误差的预测器给出、误差量在边际增益上、最坏情形保证"这个组合；表述用 "we are not aware of"。

---

## §10 本对话交付的文件（outputs）

ERROR_AND_SPINE_GUIDE.html（三把尺子 + 四问脉络，高中生版）；ERROR_RULERS_COMPARED.html（我们的 $\eta$ 与 PMAC/值空间/噪声的对照，含 toy 上三把尺子读数；注意里面 toy 数是旧版，以 §7 为准）；ABSTRACT_DRAFT_2026-09-14.md（新摘要草稿 + fallback）；J8_probe_lottery.md、J8_claude_spotcheck.py、J7_claude_spotcheck.py；hook 图各面板与 legend、algo_box（§7）；早期的 hook_figure / hook_selection 各版作废。

---

## §11 下一步（从这里接）

1. **确认状态**：git log 看 J5/J6/J7/J8 入库；REPORT 找 Q5（J8）结果；OpenReview 上摘要与标题是否已换成新版。
2. **Cici 复核**：读 app:greedybudget（J6 装配三步）和 app:hardness-anysize（J7 装配三步 + 两条初等不等式），Claude 先给逐步清单（每步一句依赖 + 一个 $K=3$ 数值示例），全打勾后标签升 [HAND-PROOF-REVIEWED]。台账更新 Prop 2(ii)(iii)。
3. **写 theory（第 3–6 节）**，每节先由 Claude 给一页 brief（必含内容、台账卡、数字、结尾答案句），Cici 写散文，定理陈述抄台账，Claude 红队。顺序：第 3 节信息价格（不依赖验证）→ 第 4 节 greedy 精确价格 → 第 6 节骨架 → 第 5 节（等 2 完成后落等号，未完成则先写 Cor K 夹逼弱版）。节标题用问题；每节首句说回答什么、尾句给答案。
4. intro 按 §6 逐句写（可与 3 并行；她的习惯是 intro 最后定稿）；Model 开头句最后定。
5. 实验节按四问分小节；两个小计算待发 Claude Code：问 0 的 value-error vs $\eta^{\mathrm{sel}}$ 散点（现有日志）、问 3 的 E3 穷举对照（greedy / 穷举 $\tilde f$ / $1/\eta$）。分母是 greedy-on-f 作 OPT 下估，穷举列可能超过 1，要说明。
6. 投稿卫生：标题决定；AI-use statement；private 仓库；Anonymous GitHub；limitation 段写 $\eta$ 可学性未证与量化噪声。
7. 不加新理论。确定性任意大小 hardness、$K\ge3$ 的严格改进留 open。

---

## §12 常见误会（新对话最容易犯）

- 不在 Model 里替 intro 说话；不预告后节；不写 "we propose predictive greedy"。
- 图 1 不标 $\eta$、不标 even/uneven；B 的全局 $\eta$ 比 A 大，图只讲 function-value error 不是决定量。
- $\eta$ 小不保证排序对；它上界翻错的代价。
- $\rho_K(\eta^{\mathrm{sel}})$ 不是保证；精确值只对全局 $\eta$。
- "同预算内没有比 greedy 好的"必须带 $|S|\le K$ 和 $n\ge n_0(K)$；J8 证明限制必要。
- J6/J7 的恒等式和 LP 已验证，装配步骤未验证；Q5 未确认。
- NW78 对固定 $K$ 不是 "no polynomial-query algorithm beats $1-(1-1/K)^K$"。
- Cohen-Addad 那条邻线实际是 Antoniadis et al. ICLR 2025。
- Prop 2(iii) 在方案二下是 level $(\eta-1)/(\eta+1)$，无 $\eta_o<2$ 前提。
- 效率：一轮一件事；她定了的直接执行。
