% ===========================================================================
% conclusion.tex -- Section 6: conclusion.  Placeholder; the human writes
% the final text.  Candidate content (draft, from RESEARCH_STATE):
% the bounded-query optimum at error eta is pinned to 1-e^{-1/eta}
% asymptotically in K, and (Q4, thm:linear-exact / ledger T10c) predictive
% greedy is now EXACTLY optimal in its own budget class A_lin at every
% n >= 4K^5 -- the former finite-K open problem for that class is closed.
% Remaining open problems: the any-size-query optimum (ledger T12), the
% randomized class at finite n, other budget scales, the exact worst case
% under a submodular surrogate (Remark \ref{rem:exact-gap}), the
% all-pairs-error version of the hardness theorem.
% ===========================================================================

\section{Conclusion}\label{sec:conclusion}
% [Placeholder -- human writes.]
