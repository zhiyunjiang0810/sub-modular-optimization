% ===========================================================================
% experiments.tex -- Section 5: experiments (G3).
%
% Hard rule for this file: NO hand-typed numeric literal in visible text.
% Every experiment number is a macro from sections/numbers.tex, generated by
%   python3 results/G3_gen_numbers.py
% and audited by
%   python3 results/G3_number_audit.py     (report: results/G3_number_audit.md)
% Budgets K are written through macros too (\EOneKMain, \ETwoKMain, ...), so
% the audit's K whitelist is not even exercised here.
%
% Table~\ref{tab:experiments} is \input from sections/EXP_table.tex, which is
% a generated copy of results/EXP_table.tex (source: results/EXP_table_build.py)
% with $\eta^{sel}$ rewritten to \etasel.  Its numbers are scripted, not
% hand-typed; the audit records the file separately for that reason.
%
% Length: budget 1.75 pages (results/G1_pagebudget.md).  The compression
% points listed there are applied: all three auxiliary figures, the protocol,
% the baseline panel and the brute-force OPT check live in
% sections/appendix_experiments.tex, and the p-sweep and out-of-model
% paragraphs are held to a few sentences each.
%
% Writing rules (CLAUDE.md, GLOSSARY.md): no em dash; the words "robust" and
% bare "tight" do not appear; "consistency" is not used at all here so it
% cannot be read outside its learning-augmented meaning; every stated
% guarantee names the error measure it is evaluated at.
% Caption drafts adapted from paper/figures/captions.tex (night 4).
% ===========================================================================

\section{Experiments}\label{sec:experiments}

The experiments ask what Proposition~\ref{prop:guarantee} certifies on
surrogates that were never built to satisfy Definition~\ref{def:eta}, and how
far that certificate sits below what predictive greedy delivers.  The
certificate is read at the measured stepwise selection error $\etasel$ of the
finished run (Remark~\ref{rem:etasel-measurable}); it applies only where the
true objective satisfies the model, which the second family does and the
first and third measurably do not, so their selection numbers are reported
as diagnostics rather than certificates.  Three surrogate families are
ordered by their distance from the model of Section~\ref{sec:model}:
surrogates \emph{learned} from data, surrogates that evaluate the same
objective on a \emph{partially observed} input, and \emph{heuristic}
surrogates for a true objective that itself lies outside the model.  The
instances constructed for Theorems~\ref{thm:exact} and~\ref{thm:tightness}
are run through the same pipeline as a control.

\paragraph{Setup.}
In every family $f$ is the quantity of interest, $\tilde f$ is what the
algorithm may query, and nothing synthetic is inserted between them.
\emph{(E1) Feature selection:} $f(S)$ is the held-out accuracy of a decision
tree trained on the features $S$, and $\tilde f(S)$ the mean
\EOneCVFolds-fold cross-validation accuracy of the same estimator on the
training split alone, so the held-out split is structurally out of the
predictor's reach; \EOneNumDatasets\ datasets (the largest with
\EOneAirlineRows\ rows), \EOneNumSeeds\ splits each,
$K=\EOneKMin,\dots,\EOneKMain$.
\emph{(E2) Influence maximization:} $f$ is one-hop coverage in a network and
$\tilde f$ the same coverage on an observed graph keeping each edge
independently with probability $p\in\{\ETwoPSet\}$
\citep{kempe2003maximizing}; \ETwoNumGraphs\ networks (largest
\ETwoLargestNodes\ nodes, \ETwoLargestEdges\ edges), \ETwoNumSeedsPerP\
observed graphs per network and $p$, giving \ETwoNumTrajectories\
trajectories of length \ETwoKMain\
\citep{rozemberczki2019gemsec,leskovec2007graph,yin2017local}.
\emph{(E3) Extractive summarization:} $f$ is the ROUGE-1 F score against a
reference summary and $\tilde f$ one of \EThreeNumSurrogates\ standard
submodular sentence-selection objectives that never see the reference
\citep{lin2011class}; \EThreeNumDocs\ articles in \EThreeNumCategories\
categories of the {BBC} news corpus \citep{greene2006practical},
$K=\EThreeKMin,\dots,\EThreeKMax$.  Ratios are
$f(S^{\tilde f})/f(S^{f})$, whose denominator is the value of greedy on the
true objective, a feasible solution and hence a lower estimate of the
optimum, so the printed ratio is an upper estimate of
$f(S^{\tilde f})/f(O^{\ast})$
(Table~\ref{tab:experiments}, note~(i)); on the one dataset small enough to
enumerate the optimum, that proxy inflates a ratio by
\EOneBreastOptInflationPct\,\%.  Greedy uses cached lazy evaluation;
accelerated variants carry guarantees of their own
\citep{mirzasoleiman2015lazier} but are not used, because measuring $\etasel$
needs the exact per-step maximizer of the true gain.
Appendix~\ref{app:experiments} gives the protocol, the baseline panel and the
enumeration.

\begin{figure}[t]
\centering
% K3 (J2 adoption): the selection-error axis carries ONLY the L_K curve.
% rho_K is a worst case for the GLOBAL error and is not an envelope over
% (etasel, ratio) pairs (exact K=2 counterexample: ratio = L_2(etasel) <
% rho_2(etasel); J2 section 5, re-verified in results/H3_j2_recheck.py),
% so it moved to Figure \ref{fig:global} on the global-eta axis.
% Source: results/K3_split_figs.py (paper-sized fonts, 7pt floor met).
\includegraphics[width=\textwidth]{figures/money_plot.pdf}
\caption{Measured ratio against the measured selection diagnostic $\etasel$
(log scale) at $K=\MoneyKLeft$ and $K=\MoneyKRight$, with the curve
$L_K(\etasel)$ of Proposition~\ref{prop:guarantee}.  The curve is a
certificate only for runs whose true objective satisfies the model
(influence maximization); for the other two families it is a reference
value at their finite-step diagnostic.  The
\ETwoHarmfulZeroTrajKMain\ influence-maximization runs whose stepwise
$\etasel$ is infinite, so that their certified bound vanishes, are stated
in the panel and not drawn.  Ratio denominators are greedy on the true objective, a lower
estimate of the optimum, so the plotted ratios are upper estimates.}
\label{fig:money}
\end{figure}

\begin{figure}[t]
\centering
% K3: global-eta axis; only the constructed instances live here, because
% only for them is the global error known by design.
\includegraphics[width=0.6\textwidth]{figures/eta_global_plot.pdf}
\caption{The exact worst case $\rho_K(\eta)$ of Theorem~\ref{thm:exact} on
the global-error axis, with the \EFourNumInstances\ constructed instances
executed by the experiment pipeline at their design errors
($K\in\{\EFourKList\}$).  Realized ratios match the theory to
$\EFourMaxDeviation$.  The global $\eta$ of the real-task runs is not
identified by the data, which is why they appear only in
Figure~\ref{fig:money}.}
\label{fig:global}
\end{figure}

\paragraph{Learned surrogates.}
Held-out accuracy is not a monotone submodular function of the feature set:
\EOneNegCandPrefixes\ of the \EOnePrefixTotal\ budget prefixes observe a
negative true candidate gain at a visited state, \EOneSelectedNegPrefixes\
select one, and \EOneHarmfulZeroPrefixes\ contain a harmful zero step, so
for this family every selection number below is a diagnostic, not a
certificate.  At $K=\EOneKMain$, over \EOneNumRunsKMain\ runs, the median
ratio is \EOneKMainRatioMedian\ (interquartile range \EOneKMainRatioLoQ\ to
\EOneKMainRatioHiQ) at median finite-step diagnostic
\EOneKMainEtaSelMedian, where the reference value
$L_{\EOneKMain}(\etasel)$ is \EOneKMainBound; on the largest dataset alone
the ratio is \EOneAirlineRatioMedian\ at diagnostic
\EOneAirlineEtaSelMedian\ (reference \EOneAirlineBound).  Which measure a
bound is read at decides whether it says anything: the trajectory error of
the same runs has median at least \EOneKMainEtaTrMedian\ (the reported
figure discards candidate pairs whose gains fall below one quantization
unit of the accuracy, so it is a lower estimate;
Appendix~\ref{app:experiments}), and at that value the same formula gives
only \EOneKMainEtaTrBound.

\paragraph{Partially observed surrogates.}
Here $f$ and $\tilde f$ are one coverage function of two nested edge sets,
both monotone submodular, so Proposition~\ref{prop:guarantee} reads as a
certificate.  At $K=\ETwoKMain$ the median ratio is \ETwoKMainRatioMedian\
(interquartile range \ETwoKMainRatioLoQ\ to \ETwoKMainRatioHiQ) at median
stepwise selection error \ETwoKMainEtaSelMedian, certifying
$L_{\ETwoKMain}(\etasel)=\ETwoKMainBound$ for the median run.
Nested coverage guarantees that predicted and true VALUES never disagree in
sign, but it does not align the zero sets of the marginal gains:
\ETwoHarmfulZeroTrajKMain\ of the \ETwoNumTrajectories\ trajectories
(\ETwoHarmfulZeroTrajPctKMain\,\%) contain a step whose chosen true gain is
zero while a positive candidate exists, their stepwise $\etasel$ is
infinite, and they certify nothing
(Table~\ref{tab:experiments}, note~(iii); \ETwoHarmfulZeroPrefixes\ budget
prefixes are affected in total).

\paragraph{Observation probability against error.}
This family lets the amount of error be dialled.  Lowering $p$ from
\ETwoPHigh\ to \ETwoPLow\ raises the median stepwise $\etasel$ monotonically
on \ETwoNumGraphsEtaMonotone\ of the \ETwoNumGraphs\ networks, by factors
from \ETwoEtaRiseMin\ to \ETwoEtaRiseMax\ between the endpoints; on the
remaining network one intermediate setting has an infinite median
(\ETwoInfMedianCells\ network-$p$ cell, where half the runs contain a
harmful zero step), which is the strongest form the degradation takes.  The
realized ratio degrades far more slowly over the same range, monotonically
on all \ETwoNumGraphsRatioMonotone\ networks, from no worse than
\ETwoRatioPHighMin\ down to between \ETwoRatioPLowMin\ and
\ETwoRatioPLowMax.  The certified bound moves fastest of the three:
$L_{\ETwoKMain}(\etasel)$ falls from the range \ETwoBoundPHighMin\ to
\ETwoBoundPHighMax\ down to \ETwoBoundPLowMin\ to \ETwoBoundPLowMax, by up
to a factor \ETwoBoundDropFactorMax\ on one network, and vanishes outright
on the infinite-median cell.  Missing observations therefore cost much less
realized value than they cost certificate.

\paragraph{Heuristic surrogates outside the model.}
At $K=\EThreeKMain$, over \EThreeNumRunsKMain\ runs, the median ratio falls
to \EThreeKMainRatioMedian\ (interquartile range \EThreeKMainRatioLoQ\ to
\EThreeKMainRatioHiQ) at median finite-step diagnostic
\EThreeKMainEtaSelMedian, where the reference value
$L_{\EThreeKMain}(\etasel)$ is \EThreeKMainBound; the best single
surrogate, the
\EThreeBestSurrogateName\ objective, reaches \EThreeBestSurrogateRatio.
Across the three families the median selection
error grows and the median ratio falls together, which is the spectrum
Table~\ref{tab:experiments} summarizes.

\paragraph{An out-of-model finding.}
The true objective of the third family does not satisfy the hypotheses of
Proposition~\ref{prop:guarantee}, and we measure by how much rather than
leaving it implicit.  On \EThreeStructTriples\ triples
$(A,\,A\cup\{b\},\,e)$ drawn from the articles with
$|A|\le\EThreeStructMaxSetSize$, the ROUGE-1 F score violates
submodularity on \EThreeSubmodViolPct\,\%\ and monotonicity on
\EThreeMonoViolPct\,\%\ of them, the largest submodularity violation being
\EThreeMaxSubmodViol\ in units of the score, while the
\EThreeNumSurrogates\ surrogates record \EThreeSurrogateViolCount\
violations of either property on the same triples; a further
\EThreeNonposPctKMain\,\%\ of trajectory steps have a nonpositive chosen
true gain, which under Definition~\ref{def:etasel} would already send the
stepwise $\etasel$ to infinity, so the printed diagnostic is the
finite-step ratio
(Table~\ref{tab:experiments}, note~(ii)).  For this family the $L_K$ column
is therefore a reference value computed by the same formula and not a
certificate, and recovering one would need a guarantee stated for
approximately submodular objectives (Section~\ref{sec:related}).

\input{sections/EXP_table}

\paragraph{The worst case, executed by the same code.}
\EFourNumInstances\ constructed instances were run through the pipeline that
produced every other point: \EFourNumVjInstances\ instances of the $V_j$
family of Theorem~\ref{thm:exact}, one per segment at $K\in\{\EFourKList\}$,
and \EFourNumUKInstances\ of the per-$K$ family of
Theorem~\ref{thm:tightness}.  Realized ratios match the theoretical values to
$\EFourMaxDeviation$, and on the $V_j$ instances the measured selection error
matches the design error to $\EFourEtaSelDeviation$.  The worst case is thus
not an asymptotic figure of speech: it occurs point by point in the same
implementation of greedy that produced the scatter of
Figure~\ref{fig:money}, and Figure~\ref{fig:global} shows the constructed
instances on the axis where their design error lives.  Three auxiliary
figures, the distribution of
$\etasel$ by budget, the observation probability sweep, and the true against
predicted gains with an error band drawn in, are deferred to
Appendix~\ref{app:experiments}.
