# TASKS9.md — 任意大小查询下的最优常数（探索夜，预算 10 小时，每项允许 FAILED）

背景：thm:hardness 只覆盖"查询集合大小 ≤ K"的算法。J5 的 N∖{e} 攻击说明现有 (x,y)-对称构造族在允许大集合查询时
泄露 O：对 e ∈ O，G(N∖{e}) = 1 − a^{n−K+τ}/(K−τ)，对 e ∉ O 则为 1。F3 的预算指数卡在 2、N4 在 τ ≥ 2 时 IIS 不可行，
都是同一障碍的不同面：F 在 y=K 处对 x 平坦（零边际）而 G 在典型大集合上必须只依赖 |S|。
今晚回答一个问题：**任意大小查询、多项式次查询的算法类，其最优常数是否仍为 1−e^{−1/η}？** 三条路线并行，
每条的"失败"都必须交出可写进论文的东西。规则不变（台账、标签、commit、Opus、45 分钟跳过）。

## P0 正确的 band 定义与可行性 LP（2 小时）
1. 写清隐藏划分论证真正需要的 O-无关区域：对大小 s 的查询，|S∩O| 的均值 sK/n、标准差 σ_s ≈ √(sK/n·(1−s/n))，
   band B(s) = {y : |y − sK/n| ≤ t·max(1, σ_s)}，t = √(2 log Q) 量级；对小 s（sK/n ≪ 1）退化为 y ≤ τ。
2. 在 (x,y) 网格上建 LP：变量 F(x,y)、G(x,y)；约束 F 单调 submodular、F(0,K)=1、单元素 η 带、
   G 在所有 (x,y) 满足 y ∈ B(x+y) 的格点上等于 Ĝ(x+y)（变量 Ĝ(s) 自由）。目标 min F(K,0)。
   参数 K ∈ {3,4,6}、n ∈ {16K, 32K, 64K}、Q = n^c、c ∈ {2,3}、η ∈ {1.5, 2, 3}。
3. 可行：记录目标值与 (F,G) 表，进入 P1 解析化。不可行：导出 IIS（最小不可行子系统），
   把它翻译成一句人话的引理（预期形如："任何只依赖 (|S∖O|,|S∩O|) 的构造，若 G 在典型大集合上 O-无关且
   F 在 y=K 有零 x-边际，则单元素带不可满足"），写成 [VERIFIED-LP 有限参数] 的 impossibility 观察。
Deliverable：results/P0_band_lp.md、脚本、IIS 证书。

## P1 非对称构造族（2.5 小时，在 P0 不可行时优先）
目标：跳出 (x,y) 计数对称。两个候选族，各建 LP：
(a) 分块隐藏：O 划分为 K 个块（或 O 与 B 各分 g 组），F、G 依赖每组的计数向量（对称性只在组内），
    G 在典型集合上依赖 (|S|, 各组计数的典型值)；检查是否消除 y=K 处的零边际泄漏（关键：F 在包含全部 O 之后
    是否仍能对 B 有与 G 匹配的正边际）。
(b) 非饱和 F：F(x,y) = 1 − a^x(1 − y/K) + φ(x)·ψ(y)，φ 递增、ψ 让 y=K 处 x-边际为正且与 Ĝ 匹配，
    同时 (K,0..τ) 处 y-边际仍在带内（N4 曾发现单一修正项在 (K,0) 破带，此处让 LP 在整族里搜）。
每族：K ∈ {3,4}、n ∈ {16K, 32K}、η ∈ {1.5, 2}，报告可行性与目标值；可行则给闭式猜测并做 (x,y) 或分块符号验证；
不可行给 IIS。同时对每个可行解跑 J5 的 N∖{e} 攻击与"所有 (n−2)-集合"攻击作泄漏检查。
Deliverable：results/P1_asymmetric_families.md。

## P2 反向：大集合查询能否让算法超过 greedy（2.5 小时）
若 hardness 难证，也许是因为它不成立。用全格点 LP 测三个使用大集合查询的算法在 K=3（n=6,7）、η ∈ {1.5, 2, 2.5}
的精确最坏值（tie 对抗，固定 K 步语义）：
(a) 反向 greedy（stingy，NWF 1978 §4 Theorem 4.4 的预测版）：从 N 开始，每步删去 f̃ 损失最小的元素，剩 K 个。
(b) 前向 greedy 与反向 greedy 各得一解，按 f̃ 取较大者。
(c) 前向 greedy，但候选评分用 d̃_e(S^t) 与 d̃_e(N∖{e}) 的几何平均（"双向评分"）。
与 ρ_3(η) 比较。任一算法在任一参数上严格超过 ρ_3：用精确有理复核、标"最需人类判断"，这将说明任意大小
查询类的最优常数可能高于 greedy，hardness 方向应放弃。全部不超过：作为"大集合查询在有限 K 也无助"的证据。
Deliverable：results/P2_large_query_algorithms.md + 脚本 + json。

## P3 汇总与论文段落（1 小时）
根据 P0–P2 的结果三选一写 results/P3_conclusion.md：
- 构造可行且解析化：定理候选陈述 + 需符号验证的恒等式清单 + transcript 装配要点（含 N∖{e} 检查通过）。
- 不可行有 IIS：写成 open-problem 段的"why it is hard"一段与一条 impossibility 观察（[VERIFIED-LP 有限参数]），
  明确它排除的是哪一族构造，不排除定理本身。
- P2 发现超过 ρ_K 的算法：写成 conjecture 与数值表，标注需要的证明。
台账：T10/T12 更新，加 must-not-claim。REPORT 顶部 5 行。commit、push。
